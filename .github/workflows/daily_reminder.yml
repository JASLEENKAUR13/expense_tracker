name: Daily Reminder
on:
  workflow_dispatch:
  schedule:
    - cron: '30 * * * *'
jobs:
  send-reminders:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Send Reminders
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          ONE_SIGNAL_APP_ID: ${{ secrets.ONE_SIGNAL_APP_ID }}
          ONE_SIGNAL_REST_API: ${{ secrets.ONE_SIGNAL_REST_API }}
        run: node .github/workflows/daily_reminder.mjs
